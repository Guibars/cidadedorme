import {MessageCircle,ArrowRight,Clock3} from 'lucide-react';
import type {PublicGameState} from '../../types';
import {Avatar3D} from '../common/Avatar3D';
export function TvDiscussionPhase({state,onAdvance}:{state:PublicGameState;onAdvance:()=>void}){
 return <div className="debate-screen"><header className="lobby-heading"><div><span className="eyebrow"><MessageCircle size={14}/>O DEBATE / RODADA {state.round}</span><h1>As histórias não batem.</h1><p>Confrontem as versões. Qualquer um pode mentir sobre quem é.</p></div><div className="debate-clock"><Clock3 size={19}/>{Math.floor(state.timerSeconds/60)}:{String(state.timerSeconds%60).padStart(2,'0')}</div></header>{state.currentEvent?.id==='sabotage_rumor'&&<div className="rumor-banner">BOATO ANÔNIMO · {state.currentEvent.description}<small>Rumores podem ser falsos. Isso não é uma prova.</small></div>}<div className="alibi-board">{state.players.filter(p=>p.isAlive).map((p,i)=><article key={p.id} className="alibi-card"><span className="eyebrow">DEPOIMENTO 0{i+1}</span><div><Avatar3D avatar={p.avatar} size="md"/><h2>{p.name}</h2></div><blockquote>“{state.answers?.find(a=>a.playerId===p.id)?.answer||'Preferiu ficar em silêncio.'}”</blockquote><small>VERSÃO DO JOGADOR · NÃO VERIFICADA</small></article>)}</div><footer className="lobby-footer"><span>Todos os sobreviventes votam. Os eliminados guardam os segredos.</span><button className="button-primary" onClick={onAdvance}>Ir para votação<ArrowRight size={16}/></button></footer></div>;
}
